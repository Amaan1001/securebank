apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: securebank
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        url: http://prometheus-service:9090
        isDefault: true
        access: proxy
        editable: true
